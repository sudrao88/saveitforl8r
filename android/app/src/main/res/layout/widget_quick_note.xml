<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical">

    <!-- App icon (foreground only, no adaptive icon circle) -->
    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginEnd="8dp"
        android:src="@mipmap/ic_launcher_foreground"
        android:contentDescription="@string/app_name"
        android:scaleType="centerCrop" />

    <!-- Tappable text area that opens the app -->
    <TextView
        android:id="@+id/widget_input_hint"
        android:layout_width="0dp"
        android:layout_height="38dp"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:text="@string/widget_hint"
        android:textColor="#9CA3AF"
        android:textSize="14sp"
        android:background="@drawable/widget_input_background"
        android:singleLine="true"
        android:ellipsize="end" />

    <!-- Camera quick action -->
    <ImageView
        android:id="@+id/widget_camera_btn"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="10dp"
        android:src="@drawable/ic_widget_camera"
        android:contentDescription="@string/widget_camera"
        android:padding="10dp"
        android:background="?android:attr/selectableItemBackgroundBorderless" />

    <!-- Document upload quick action -->
    <ImageView
        android:id="@+id/widget_document_btn"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="6dp"
        android:src="@drawable/ic_widget_document"
        android:contentDescription="@string/widget_document"
        android:padding="10dp"
        android:background="?android:attr/selectableItemBackgroundBorderless" />

</LinearLayout>
