<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. DayNight so the WebView's prefers-color-scheme
         follows the OS night-mode setting — this is what makes the in-app
         "System" theme option actually track the device theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Initial icon style before the web layer sets it at runtime via the
             StatusBar plugin; overridden per resolved theme there. -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- SplashScreen theme with proper API configuration for Android 12+ -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Background color of the splash screen -->
        <item name="windowSplashScreenBackground">#000000</item>
        <!-- No icon — plain black splash screen -->
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <!-- Theme to apply to the activity after splash -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
